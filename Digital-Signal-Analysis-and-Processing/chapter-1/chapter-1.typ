= Discrete Time Signals and Systems
Signal is defined  as any physical quantity that varies with time,space or any other independent variable.
It's types are:
+ *Continuous Time Signal*: A signal can be defined in evet instance of time under consideration. It is denoted as x(t) where t is a continuous variable and can assume values in the range -#sym.infinity #sym.lt.eq t #sym.lt.eq #sym.infinity.
+ *Discrete Time Signal*: A signal which is defined only at certain time instants is known as discrete time signal. It is represented as x[n], where n assumes discrete values.

==  Basic Signal Types
+ * Unit Impulse Signal*: Denoted by #sym.delta [n], it is defined as:
  